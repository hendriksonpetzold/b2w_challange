import 'package:flutter/material.dart';

class HomeBestSellerListView extends StatelessWidget {
  const HomeBestSellerListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
